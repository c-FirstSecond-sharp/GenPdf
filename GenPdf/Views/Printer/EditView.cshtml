@model GenPdf.PDFPrinter

@{
    ViewData["Title"] = "EditView";
}

<h1>EditView</h1>

<iframe src="@Url.Action("Index", "Printer")" name="foo" width="600" height="400"></iframe>
<h4>PDFPrinter</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditView">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*<div class="form-group">
                <label asp-for=@Model.ID class="control-label"></label>
                <input asp-for=@Model.ID class="form-control" readonly />
                <span asp-validation-for=@Model.ID class="text-danger"></span>
            </div>*@
            <div class="form-group">
                <label asp-for="Sentence" class="control-label"></label>
                <input asp-for="Sentence" class="form-control" />
                <span asp-validation-for="Sentence" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Text color</label>
                @Html.DropDownList("TextColor", @Model.ColorList)
            </div>
            <div class="form-group">
                <label class="control-label">Rectangle color</label>
                @Html.DropDownList("RectangleColor", @Model.ColorList)
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
